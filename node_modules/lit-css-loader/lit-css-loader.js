import { transform } from "@pwrs/lit-css";
const loader = function loader2(css) {
  const callback = this.async();
  const filePath = this.resourcePath;
  const options = this.getOptions();
  const { specifier, tag, uglify, ...rest } = options ?? {};
  transform({ css, specifier, tag, uglify, filePath, ...rest }).then((result) => {
    callback(null, result);
  }).catch((err) => {
    callback(err, css);
  });
};
var lit_css_loader_default = loader;
export {
  lit_css_loader_default as default
};
//# sourceMappingURL=lit-css-loader.js.map
